# React Redux Firebase Post App

Simple application to create posts in firebase using react, redux and router

## Usage

`npm install`

To run : `npm start` & navigate to [http://localhost:8080/app](http://localhost:8080/app)
